'use client';

// This imports the main shift scheduler app
// The full app component from the artifact is placed in src/app/ShiftScheduler.jsx
import ShiftScheduler from './ShiftScheduler';

export default function Home() {
  return <ShiftScheduler />;
}
